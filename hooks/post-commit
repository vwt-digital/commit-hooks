#!/bin/bash
REMOTES=""
while read -r line; do
    if [ -z "$REMOTES" ]
    then
          REMOTES="${line}"
    else
          REMOTES="${REMOTES}\|${line}"
    fi
done < "${0%/hooks/*}/remotes"

remote_spec=$(git remote -v | grep "$(git remote)\s.*(push)$")

if [ -n "$(echo "${remote_spec}" | grep "$REMOTES")" ]; then
  git_dir=$(git rev-parse --show-toplevel)
  branch_name=$(git branch | grep \* | cut -d ' ' -f2)
  exit_code=0

  hook_branch=$(cd "$(dirname "$0")"/.. && git rev-parse --abbrev-ref HEAD)

  echo "Running post-commit hook on branch ${branch_name}: "
  ##############################################################################
  ##########                         SAST scan                        ##########
  ##############################################################################
  echo "SAST scan..."

  if [[ $hook_branch == "develop" ]]; then
    docker_image="eu.gcr.io/vwt-d-gew1-dat-cloudbuilders/cloudbuilder-sast"
  else
    docker_image="eu.gcr.io/vwt-p-gew1-dat-cloudbuilders/cloudbuilder-sast"
  fi

  mkdir tmp-post-commit-folder > /dev/null 2>&1

  for file in $(git diff-index --name-only --diff-filter=ACMT --cached HEAD -- ); do
    cp --parents "$file" tmp-post-commit-folder
  done
  [ -d "$git_dir"/node_modules ] && cp -r "$git_dir"/node_modules tmp-post-commit-folder
  cp -r "$git_dir"/.git tmp-post-commit-folder
  if [[ -d sast-config ]]; then
    cp -r "$git_dir"/sast-config tmp-post-commit-folder
  fi

  docker run --volume "$git_dir"/tmp-post-commit-folder:/sast-files $docker_image --context post-commit || exit_code=1

  # Remove temporary folder
  rm -rf tmp-post-commit-folder

  if [ "$exit_code" != 0 ] ; then
    git reset HEAD^ --soft
    echo
    echo -e "\033[0;31mError: Trufflehog found one or more secrets in your commit, and it has been reverted.
  Please review the error generated by Trufflehog and remove the secret(s). For deliberately commited secrets, add an ignore rule.  \033[0m
  For more information on how to do this, see: https://github.com/vwt-digital/cloudbuilder-sast#excluding-false-positives"
    exit 1
  fi
fi